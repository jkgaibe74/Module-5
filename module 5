#Module 5 - Doing Math
#Find the value of inverse of a matrix, determinant of a matrix 
#by using the following values:

A <- matrix(1:100, nrow=10)  
B <- matrix(1:1000, nrow=10)


#finding inverse and calculate the determinant 
solve(A)
det(B)
